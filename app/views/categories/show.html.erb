<p id="notice"><%= notice %></p>

<h2 style="color:<%=@category.color%>;">Articulos de <%=@category.name%> </h2>

<div class="row">
<%= render @category.articles%>
</div>

<%= link_to 'Edit', edit_category_path(@category) %> |
<%= link_to 'Back', categories_path %>
